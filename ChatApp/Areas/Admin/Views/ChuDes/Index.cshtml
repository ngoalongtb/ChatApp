@model IEnumerable<ChatApp.EF.ChuDe>

@{
    /**/

    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/AdminMaster.cshtml";
}

<section class="content container-fluid">
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Danh sách chủ đề</h3>
            <h3>@Html.ActionLink("Tạo mới", "Create")</h3>
        </div>
        <!-- /.box-header -->
        <div class="box-body">
            <table id="example1" class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.TenChuDe)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.DanhMuc.TenDanhMuc)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.TenChuDe)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DanhMuc.TenDanhMuc)
                            </td>
                            <td>
                                @Html.ActionLink("Sửa", "Edit", new { id = item.MaChuDe }) |
                                @Html.ActionLink("Chi tiết", "Details", new { id = item.MaChuDe }) |
                                @Html.ActionLink("Xoá", "Delete", new { id = item.MaChuDe })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

@section script{
    <script>
        $(function () {
            $('#example1').DataTable()
            $('#example2').DataTable({
                'paging': true,
                'lengthChange': false,
                'searching': false,
                'ordering': true,
                'info': true,
                'autoWidth': false
            })
        })
    </script>
}
